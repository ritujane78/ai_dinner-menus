spring:
  application:
    name: dinners-menu_ai
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      audio:
        speech:
          options:
            model: gtp-4o-mini-tts
            voice: alloy
            format: mp3
            speed: 1.2
